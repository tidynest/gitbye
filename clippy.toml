# Product names that read as prose in documentation, not as code identifiers.
# ".." keeps clippy's own default list, which already covers GitHub and GitLab.
doc-valid-idents = ["PostgreSQL", "SemVer", "WCAG", ".."]
